% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metamet.R
\name{metamet}
\alias{metamet}
\alias{metamet.default}
\alias{metamet.character}
\alias{metamet.data.frame}
\alias{metamet.data.table}
\title{Create a `metamet` object from various input types}
\usage{
metamet(dt, ...)

\method{metamet}{default}(dt, ...)

\method{metamet}{character}(
  dt,
  dt_meta = NULL,
  dt_site = NULL,
  dt_qc = NULL,
  dt_ref = NULL,
  site_id = NULL,
  ...
)

\method{metamet}{data.frame}(
  dt,
  dt_meta = NULL,
  dt_site = NULL,
  dt_qc = NULL,
  dt_ref = NULL,
  site_id = NULL,
  ...
)

\method{metamet}{data.table}(
  dt,
  dt_meta = NULL,
  dt_site = NULL,
  dt_qc = NULL,
  dt_ref = NULL,
  site_id = NULL,
  ...
)
}
\arguments{
\item{dt}{The main data input. For `metamet.character`, this is a file path
to a CSV or other format readable by `data.table::fread()`. For
`metamet.data.frame` or `metamet.data.table`, this is the actual data object.}

\item{...}{Additional arguments passed to methods.}

\item{dt_meta}{A `data.table`, `data.frame`, or file path to metadata describing
the columns in `dt`. Excel files (.xlsx) are supported; otherwise CSV format
is assumed. Required.}

\item{dt_site}{A `data.table`, `data.frame`, or file path to site metadata
containing site-level information. Required.}

\item{dt_qc}{Optional quality control data; can be a `data.table`, `data.frame`,
or file path. Defaults to `NULL`.}

\item{dt_ref}{Optional reference data (e.g., ERA5 reanalysis); can be a
`data.table`, `data.frame`, or file path. Defaults to `NULL`.}

\item{site_id}{A character string specifying the site identifier.
Required for `metamet.character` method. Used to tag all observations.}
}
\value{
A `metamet` object containing the provided data and metadata,
  with processed and validated structure.
}
\description{
Constructor function for creating `metamet` objects from files or
data tables. This is a generic function with methods for different
input types, allowing flexible object creation from CSV/Excel files,
data frames, or data tables.
}
\examples{
\dontrun{
# Create from files
mm <- metamet(
  dt = "data.csv",
  dt_meta = "metadata.xlsx",
  dt_site = "site_info.csv",
  site_id = "UK-AMO"
)

# Create from data tables (requires metadata and site data)
mm <- metamet(
  dt = my_data_table,
  dt_meta = my_metadata_table,
  dt_site = my_site_info_table,
  site_id = "UK-AMO"
)
}

}
\seealso{
\code{\link{new_metamet}} for the internal constructor
}
